# GreenV Vegetation Prediction API (Phase 12 integration).
# Local dev default matches services/greenv-vegetation-prediction's documented CORS origins
# (reports/api.md) for this app's Vite dev server. Copy this file to .env.local and adjust if the
# prediction API runs elsewhere -- never commit a real .env file.
VITE_VEGETATION_PREDICTION_API_URL=http://127.0.0.1:8000
